-- 5/12/2025 --
NOTE: system breaks when time of work (TOW) per slot exceeds the SECONDS_PER_TICK.

Emptying the callback queue MUST finish before the current tick is over -> HARD REQUIREMENT

TTW/slot is determined almost completely by the update_time(secret) X num(calls)/slot.

Seconds per tick must accomodate TOW/slot, because if workers go out of phase with 
the main tick loop, the could re-schedule for ticks behind current tick.

I should experiment with the SECONDS_PER_TICK and NUM_WORKER to increase system capacity.

----

Add append only log table which refresh has succeeded or not 
    -> need to know if endpoint is dead, credentials are not valid, etc.

-----

[1] token client in kaart brengen wat te testen voor core
en waar de huidige risico's en edge cases liggen

[2] opzet maken voor failsafe/failover op edge cases / 
problemen met calls / etc.

[3] set_secret in callback zetten + code cleaning

[4] translatie van seconds -> ticks en terug management uitwerken



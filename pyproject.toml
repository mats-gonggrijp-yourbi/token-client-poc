[project]
name = "token-client-poc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["fastapi", "uvicorn", "python-multipart"]
[dependency-groups]
dev = [
    "jupyter",
]
